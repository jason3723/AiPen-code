# 文档批注功能 · 手动验证 Checklist

> 版本：W5 收尾后的最终交付。逐项跑完打 ✅ / ❌，遇到 ❌ 把复现步骤与日志贴在备注。
> 启动方式：在 `d:\Project\AiPen` 目录运行 `npm run tauri dev`（或你的常规启动方式）。

## 0. 启动与基础确认

- [ ] **0.1** 启动 AiPen，能正常进入主界面，左栏显示「文档」列表
- [ ] **0.2** 创建一个新文档（点击左栏「+ 新建文档」或顶栏按钮）
- [ ] **0.3** 文档标题重复验证：再创建一个同名文档，后建的标题应自动带 `（1）` 后缀
- [ ] **0.4** 输入几段测试文字（至少 3 段，每段 1~3 句）作为测试样本

---

## 1. 工具栏与右键入口

- [ ] **1.1** 工具栏出现 💬 批注按钮（高亮按钮旁、清除格式按钮前）
- [ ] **1.2** 在素材模式下，工具栏**不**显示 💬 批注按钮
- [ ] **1.3** 选中文本 → 右键菜单出现「💬 插入批注」入口
- [ ] **1.4** 空选区时点击工具栏 💬 批注 → 顶部出现「请先选中要批注的文字」提示
- [ ] **1.5** 跨段选区时点击 💬 批注 → 顶部出现「批注暂不支持跨段…」提示
- [ ] **1.6** 单段内选区时点击 💬 批注 → 弹出输入浮层（光标已自动聚焦 textarea）

---

## 2. 插入批注

- [ ] **2.1** 在浮层中输入批注文字（≤ 500 字）→ 点「✓ 保存」或 Ctrl+Enter
- [ ] **2.2** 选中的文字立刻出现**下划虚线**（琥珀色 dashed）
- [ ] **2.3** 选中文末尾出现上角标 `[1]`
- [ ] **2.4** 另一处选中文本 → 插入第 2 条批注 → 角标为 `[2]`（自动递增）
- [ ] **2.5** 再插入第 3 条 → 角标为 `[3]`
- [ ] **2.6** 浮层中输入超 500 字 → 输入框应在 500 处截断（计数器显示 `500/500`）
- [ ] **2.7** 浮层中按 Esc 或点外部 → 浮层关闭，未生成批注
- [ ] **2.8** 浮层点「取消」→ 浮层关闭，未生成批注

---

## 3. 草稿自动保存

- [ ] **3.1** 插入 1 条批注后，看底部状态栏 → 应显示「● 草稿已自动保存（时间）」
- [ ] **3.2** 关闭 AiPen（窗口 ×）→ 重新启动 → 打开同一文档 → 批注、角标、下划虚线全部保留

---

## 4. 提交版本与历史回滚

- [ ] **4.1** 在顶栏「提交信息」输入 `初稿` → 点「📌 提交版本」
- [ ] **4.2** 右栏切到「版本」标签 → 看到 v1 备注为 `初稿`
- [ ] **4.3** 删掉其中 1 条批注（文末列表 🗑）→ 提交版本 `v2 删了1条`
- [ ] **4.4** 在 v2 上再插 1 条新批注 → 角标应为 `[3]`（不回收旧编号）
- [ ] **4.5** 在 v2 上又修改一条已有批注的文本（文末列表 ✎）
- [ ] **4.6** 提交版本 `v3 改文字`
- [ ] **4.7** **回滚到 v1**：v1 上点 ↺ → 确认 → 编辑器显示原内容 + 3 条原批注 + 原角标
- [ ] **4.8** 切到「Diff」标签：选 v1 + v3 → 点「开始对比」→ 看到增/删/变更统计与具体差异
- [ ] **4.9** **回滚到 v3** → 文档恢复到最新草稿状态（含修改后的文字 + v3 的批注集合）
- [ ] **4.10** 关键断言：v1 视图里**仍含**3 条原始批注（即使 v2/v3 已删除部分）—— 历史版本不可变

---

## 5. 撤销 / 重做

- [ ] **5.1** 在 v3 最新草稿上操作：
  - 选一段 → 插批注 → 角标出现
  - 按 Ctrl+Z → 角标消失，文字下划虚线消失（**一次撤销**完成整套动作）
- [ ] **5.2** 按 Ctrl+Y → 批注重新出现
- [ ] **5.3** 连续 5 次 Ctrl+Z → 撤销最近 5 个动作
- [ ] **5.4** 连续 5 次 Ctrl+Y → 全部恢复

---

## 6. 孤儿批注

- [ ] **6.1** 选一段带批注的文字所在的整段 → 整段删除（包括批注）
- [ ] **6.2** 该段下划虚线与角标消失（doc 中已无 mark）
- [ ] **6.3** 文末「📝 批注」列表中该条仍在，标灰显示「⚠ 原文已删除」
- [ ] **6.4** 列表底部汇总显示「(N) 含 M 条孤儿」
- [ ] **6.5** 孤儿批注的「📍 跳转」按钮置灰禁用
- [ ] **6.6** 孤儿批注可继续编辑（✎）和删除（🗑）
- [ ] **6.7** 提交版本（含 1 条孤儿）→ 新版本中文末列表也保留该孤儿

---

## 7. hover 浮层

- [ ] **7.1** 鼠标悬停**带下划虚线**的文字 → 浮层弹出，显示批注内容
- [ ] **7.2** 鼠标悬停**上角标** `[1]` → 浮层同样弹出
- [ ] **7.3** 浮层显示：批注文本 + 作者（"我"）+ 时间（精确到分）
- [ ] **7.4** 浮层不显示在视口外（自动向上翻转 / 左右贴边）
- [ ] **7.5** 鼠标移入浮层本身 → 不立即关闭（150ms 延迟）
- [ ] **7.6** 鼠标从文字移到浮层外 → 浮层关闭
- [ ] **7.7** 浮层「📍 跳转」→ 关闭浮层 + 滚动到原位置（如果原文还在）
- [ ] **7.8** 浮层「✎ 编辑」→ 关闭浮层，文末列表对应条目进入编辑态
- [ ] **7.9** 浮层「🗑 删除」→ 弹确认框 → 确认后批注 + mark + 角标同步消失
- [ ] **7.10** 悬停孤儿批注的文字 → **不**弹浮层（孤儿无 anchor）

---

## 8. 文末批注列表

- [ ] **8.1** 文档无批注时，文末**不**显示「📝 批注」面板
- [ ] **8.2** 有批注时，面板按 `order` 升序列出（[1] [2] [3]…）
- [ ] **8.3** 列表标题显示 `📝 批注 (N)`，N 为**非孤儿**条数
- [ ] **8.4** 列表面板可折叠（▶ / ▼ 切换）
- [ ] **8.5** 「📍 跳转」按钮在非孤儿批注上可用，孤儿上禁用
- [ ] **8.6** 「✎ 编辑」：该条进入内联编辑态（textarea），其他操作按钮替换为 ✓ / ✕
- [ ] **8.7** 编辑中按 Esc → 取消编辑
- [ ] **8.8** 编辑中按 Ctrl+Enter 或点 ✓ → 保存修改
- [ ] **8.9** 「🗑 删除」→ 弹 useConfirm 确认框（与其他模块一致）→ 确认后从列表 + mark + 角标同步移除

---

## 9. Word 导出

- [ ] **9.1** 顶栏「📄 导出 Word」→ 选保存路径 → 生成 .docx
- [ ] **9.2** 用 Word/WPS 打开 .docx → 文中每个带批注的位置后跟有**上角标** `[1]`（琥珀色）
- [ ] **9.3** 文档末尾出现「批注」标题（居中，H2 样式）
- [ ] **9.4** 标题下逐条列出批注：`[1] 批注内容` `[2] 批注内容` …
- [ ] **9.5** 孤儿批注在 Word 中标灰、斜体、末尾加「（原文已删除）」
- [ ] **9.6** 批注按 order 升序排列
- [ ] **9.7** 整篇无批注时，Word 末尾**不**出现「批注」章节
- [ ] **9.8** 字体 / 字号 / 排版与导出设置一致（已有逻辑不受影响）

---

## 10. 备份导出

- [ ] **10.1** 右栏「⚙ 设置」→ 滚到「数据管理」
- [ ] **10.2** 6 个勾选按钮中，「文档」按钮的 tooltip 提示「含批注自动同步」
- [ ] **10.3** 仅勾选「文档」→ 点「📦 导出选中数据」→ 选路径 → 成功提示包含「（含 N 条批注）」
- [ ] **10.4** 打开 .aipen 文件（编辑器或 JSON 工具）→ 找到 `_meta` → 含 `contains_comments: true`、`comment_count`、`min_reader_version: "2.0.0"`、`exported_by_version`
- [ ] **10.5** 备份导出含孤儿批注时，`_meta.orphan_comment_count > 0`

---

## 11. 备份导入

- [ ] **11.1** 把上一步导出的 .aipen 复制到另一台同版本 AiPen（或清空数据库模拟）
- [ ] **11.2** 点「📥 导入数据」→ 选文件 → 成功提示显示导入文档数
- [ ] **11.3** 打开导入的文档 → 批注、角标、下划虚线、文末列表全部完整
- [ ] **11.4** **跨版本兼容（手工测试）**：在 `compareVersion` 临时把 `min_reader_version` 改为 `99.0.0` 重新构建 → 导入带批注的备份 → 成功提示应追加 `⚠ 此备份含 N 条批注，当前版本 X 低于最低兼容 99.0.0，批注将丢失。`
- [ ] **11.5** **极简容错（手工测试）**：手动删除 .aipen 里的 `comments` 字段 → 导入 → 不报错，批注丢失（其余数据正常）

---

## 12. 文档切换 / 跨文档

- [ ] **12.1** 在 doc A 插入 2 条批注 → 切到 doc B → doc B 状态干净（无批注）
- [ ] **12.2** doc B 输入几段文字 → 切回 doc A → 批注、角标、文末列表全部恢复
- [ ] **12.3** 在 doc A 提交版本 v1 → 切到 doc B 提交 v1 → 两个文档各有自己的 v1
- [ ] **12.4** 在 doc A 看历史（v1 视图）→ 切到 doc B → doc A 的 v1 视图状态正确退出（编辑器为 doc B 最新草稿）

---

## 13. 边界与异常

- [ ] **13.1** 同一段内**重复插入** 5 条批注（5 个不同选区）→ 角标 [1][2][3][4][5] 全部出现
- [ ] **13.2** 选中整段 → 插批注 → 整段加粗（Ctrl+B）→ 批注 mark 仍在，角标仍在（mark 跨多个 inline 节点兼容）
- [ ] **13.3** 选区含中英混合 + 标点 → 批注边界正确
- [ ] **13.4** 文档有批注时，删除文档 → 文档连同所有批注（**通过 store 字段持久化的所有内容**）一起消失，无残留
- [ ] **13.5** 在 hover 浮层打开状态下切换文档 → 浮层立即关闭，新文档无残留 hoveredCommentId

---

## 14. 性能

- [ ] **14.1** 单文档插入 50 条批注 → 编辑器流畅（输入不卡顿）
- [ ] **14.2** 单文档插入 50 条批注 → 文末列表渲染流畅（折叠/展开无明显延迟）
- [ ] **14.3** 切换文档响应 < 200ms（无明显等待）
- [ ] **14.4** 提交版本响应 < 1s（无 50 条批注影响）

---

## 15. 视觉与无障碍（可选）

- [ ] **15.1** 切换深色主题 → 下划虚线、角标、浮层、列表配色协调可读
- [ ] **15.2** 浅色主题下文字 + 角标 + 浮层对比度足够
- [ ] **15.3** 鼠标 cursor: help 在批注文字上正确显示

---

## 测试通过标准

- 第 1~13 节 **所有**项打 ✅
- 第 14~15 节**可接受**少量 ❌（性能 / 视觉无硬性要求，但若明显卡顿或看不清需修）

## 失败反馈模板

```
❌ [编号 + 简短标题]
复现步骤：
  1. …
  2. …
  3. …
预期：
  …
实际：
  …
控制台/日志：
  …
```

---

## 附录 A：关键代码位置速查

| 关注点 | 文件 / 行 |
|---|---|
| `comment` Mark 定义 | `src/components/RichEditor.vue` → `CommentMark` |
| 角标 Decoration 插件 | `src/components/RichEditor.vue` → `CommentBadgeExt` |
| 浮层入口（工具栏 / 右键） | `execComment` + `execCtxMenuAddComment` |
| 插入确认 | `onCommentConfirm` |
| 列表跳转 | `onCommentListJump` |
| 孤儿扫描 | `docStore.sweepOrphans` + `onUpdate` 末尾 |
| Ghost mark 自动清理 | `comments` watch + `onUpdate` ghostIds 分支 |
| 备份元数据 | `ApiSettings.vue` → `handleExport` / `handleImport` |
| Word 导出角标 | `exportWord.ts` → `buildTextRuns` `case 'comment'` |
| Word 导出文末列表 | `exportWord.ts` → `buildDocx` 末尾 |
| 类型定义 | `src/types/comment.ts` |
| 路线图 / 测试矩阵 / 设计 | `docs/批注功能技术方案.md` |
